import type { AllowedDurationMinutes } from "./types";

export const ALLOWED_DURATIONS: AllowedDurationMinutes[] = [5, 10, 15, 30];

export const DEFAULT_PAUSE_MINUTES = 5;

export const EXTEND_MINUTES = 5;
