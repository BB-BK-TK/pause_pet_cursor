import { COPY } from "./copy";
import type { FocusDurationMinutes } from "./types";

export { COPY };

export const FOCUS_DURATIONS: FocusDurationMinutes[] = [5, 10, 15, 25];

export const DEFAULT_PAUSE_MINUTES: FocusDurationMinutes = 5;
